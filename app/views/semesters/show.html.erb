<p style="color: green"><%= notice %></p>

<%= render @semester %>

<div>
  <div class="submit"><%= button_to "Destroy this semester", @semester, method: :delete %></div>

  <div class="edit_nav_button">
    <%= link_to "Edit this semester", edit_semester_path(@semester) %> |
    <%= link_to "Back to semesters", semesters_path %>
  </div>
</div>
